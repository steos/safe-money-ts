{
  "name": "safe-money",
  "version": "1.0.0-beta.0",
  "description": "safe-money in typescript, based on: https://ren.zone/articles/safe-money",
  "license": "BSD-3-Clause",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "ava src/* -v --timeout=1m",
    "prepublish": "tsc",
    "build": "tsc",
    "typedoc": "typedoc --out docs src/index.ts --exclude **/*.test.ts --readme none --mode file --theme minimal --excludePrivate"
  },
  "dependencies": {},
  "devDependencies": {
    "ava": "^3.5.2",
    "ts-node": "^8.8.1",
    "typedoc": "^0.17.7",
    "typescript": "3.8.3"
  },
  "ava": {
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  }
}
